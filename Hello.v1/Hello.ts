console.log("Mój pierwszy projekt z automatyczna kompilacją i nodemon+ zmiana folderów");
console.log("Mój pierwszy projekt z automatyczna kompilacją i nodemon+ zmiana folderów");

function powitanie(imię: string, nazwisko: string, wiek: number): string {
    return `Cześć, 
${imię} ${nazwisko}
masz ${wiek} lat
Przed Tobą CAŁY ŚWIAT!`;
}

console.log(powitanie(`Miłosz`, `Gołas`, 35));